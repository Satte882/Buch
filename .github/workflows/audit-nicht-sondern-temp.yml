name: Temporary Nicht-sondern audit
on:
  push:
    paths:
      - scripts/audit_nicht_sondern_temp.py
      - .github/workflows/audit-nicht-sondern-temp.yml
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Audit Nicht-sondern family
        run: python scripts/audit_nicht_sondern_temp.py
