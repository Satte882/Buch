name: Temporary style depth audit

on:
  push:
    paths:
      - scripts/style_depth_audit_temp.py
      - .github/workflows/style-depth-audit-temp.yml
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Audit Heller ambiguity, style formulas and prose variance
        run: python scripts/style_depth_audit_temp.py
