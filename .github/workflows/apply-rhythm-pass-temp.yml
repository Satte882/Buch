name: Apply temporary rhythm pass

on:
  push:
    paths:
      - scripts/apply_rhythm_pass_temp.py
      - .github/workflows/apply-rhythm-pass-temp.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply curated rhythm pass
        run: python scripts/apply_rhythm_pass_temp.py
      - name: Validate master structure
        shell: python
        run: |
          from pathlib import Path
          import re
          text = Path('AUSNAHMEZUSTAND_FINAL.md').read_text(encoding='utf-8')
          assert re.findall(r'^## (Prolog|\d+)$', text, re.M) == ['Prolog'] + [str(i) for i in range(1, 48)]
          assert text.rstrip().endswith('„Wie belastbar ist deine Gegenhypothese?“')
          assert '—' not in text
      - name: Commit manuscript rhythm pass
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add AUSNAHMEZUSTAND_FINAL.md
          git commit -m "Refine manuscript dialogue rhythm"
          git pull --rebase origin main
          git push
